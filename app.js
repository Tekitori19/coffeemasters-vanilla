window.addEventListener("DOMContentLoaded",() => {
    let nav = document.querySelector("nav");
    console.log(nav);
    // nav.innerHTML =`
    //     <h1>This is nav</h1>
    //     <p>This is param</p>
    // `;
})
